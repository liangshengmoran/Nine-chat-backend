# =============================================
# Nine-Chat 后端环境配置模板
# 复制此文件为 .env 并填写相应配置
# =============================================

# ============ 服务器配置 ============
# 服务端口
PORT=5000
# Swagger 文档路径前缀
PREFIX=/docs

# ============ 数据库配置 ============
# 数据库类型: sqlite 或 mysql
# SQLite: 无需额外配置，开箱即用，适合开发和小型部署
# MySQL: 需要配置下方 DB_HOST 等参数，适合生产环境
DB_TYPE=sqlite

# SQLite 数据库文件路径 (仅 DB_TYPE=sqlite 时有效)
DB_SQLITE_PATH=./data/nine-chat.sqlite

# MySQL 数据库配置 (仅 DB_TYPE=mysql 时有效)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=your_password
DB_DATABASE=your_sql_name

# ============ 音乐 API 配置 ============
# 酷狗音乐 API 地址
# 可使用公共 API 或自建服务
KUGOU_API_BASE=your_kugou_music_api

# 酷狗验证参数 (用于需要登录的接口，如获取播放地址、每日推荐等)
# 获取方式: 登录酷狗网页版，从请求头中获取 token 和 userid
KUGOU_TOKEN=your_kugou_music_token
KUGOU_USERID=your_kugou_music_userid

# 网易云音乐 API 地址
# 可使用公共 API 或自建 NeteaseCloudMusicApi 服务
NETEASE_API_BASE=your_netease_music_api

# ============ JWT 配置 (可选) ============
# JWT 密钥，用于生成和验证 token
# 如不配置，将使用默认密钥
# JWT_SECRET=your_jwt_secret_key

# ============ 其他配置 (可选) ============
# 文件上传大小限制 (MB)
# UPLOAD_MAX_SIZE=10

# 是否开启调试日志
# DEBUG=false